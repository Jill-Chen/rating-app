.login-page
  form.well.login(action='/user/edit', method="post")
    h3= title
    - if('undefined' !== typeof errors && errors.length)
      .alert-message.warning(data-alert)
        a.close(href='#') ×
        - each error in errors
          p.error= error
    label(for=everyauth.password.loginFormFieldName) 邮箱：
    input#input_login(type='text',value=user.email, disabled)

    label(for="name") 花名或真名：
    input.text#name(type='text',autofocus='autofocus',value=user.name, name="name")

    .form-actions
      button(type="submit", class='btn primary') 提交
      &nbsp;
      a(href='/') 返回分享平台
