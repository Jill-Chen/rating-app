h1 All Rates
#page
- if (result.length)
  ul.ratelist
    - result.forEach(function(item){
    li
      p.title
        a(href="/show/#{item._id}")=item.title

      p.name
        span=item.author

      - if (item.rates)
        div.rate
          - item.rates.forEach(function(r){
            span.item=r
          - })
          span=item.value
      p.id _id :
        span=item._id

      p.actions
        a(href="/show/#{item._id}") 打开

      p.score=item.score
    - })
- else
  p result is empty
