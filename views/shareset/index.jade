// 分享会列表
#page
  .row
    ul.pills
  .calendar
    .hd
      h3 #{current.format("YYYY 年 M 月")}
      .act
        a.btn(href='/shareset') 当月
        a.btn(href='/shareset/?month=#{current.add('months',-1).format('YYYY-MM')}') 上月
        a.btn(href='/shareset?month=#{current.add('months',2).format('YYYY-MM')}') 下月
    .weekday
      span.Sun 周日
      span 周一
      span 周二
      span 周三
      span 周四
      span 周五
      span 周六
    - each day in days
      .day(class="#{day.moment.format('ddd')} #{day.current?'current':''}#{day.today?'today':''} ")
        .date= day.moment.format('D')
        ul.sharesets
          - each shareset in day.sharesets
            li
              a(href='/shareset/#{shareset.postname}')= shareset.subject
